server:
  type: simple
  applicationContextPath: /
  adminContextPath: /admin
  connector:
    type: http
    port: 9000

salesforceConfiguration:
  name: Salesforce
  secret: "password"
  apiUrl: "http://localhost:9000/test/response"

companiesHouseConfiguration:
  name: CHIPS
  secret: "password"
  apiUrl: "http://localhost:9000/test/submission"
  barcodeServiceUrl: "http://localhost:9000/test/barcode"

jerseyClient:
  timeout: 8000ms
  chunkedEncodingEnabled: false
  gzipEnabled: false
  gzipEnabledForRequests: false

log4jConfiguration:
  frequency: 1
  # Valid values are DAYS HOURS MICROSECONDS MILLISECONDS MINUTES NANOSECONDS SECONDS
  timeUnit: "HOURS"
